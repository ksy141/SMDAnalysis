* GENERATED BY CHARMM-GUI (http://www.charmm-gui.org) v3.1 on Nov, 14. 2020. JOBID=0540241948
* BUILD REF FILE FOR NAMD EQUILIBRATION
*

! keep the lipid head groups close to target values
define HEAD sele none end
define HEAD sele segid MEMB .and. resn chl1 .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chl1 .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn erg .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/erg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn erg .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/erg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sito .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sito_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sito .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sito_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn stig .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/stig_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn stig .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/stig_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chsd .and. ( type C3 .or. type O3 .or. type C4' .or. type O2' .or. type C5' ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chsd_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chsd .and. ( type C3 .or. type O3 .or. type C4' .or. type O2' .or. type C5' ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chsd_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chsp .and. ( type C3 .or. type O3 .or. type C4' .or. type O2' .or. type C5' ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chsp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chsp .and. ( type C3 .or. type O3 .or. type C4' .or. type O2' .or. type C5' ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chsp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn camp .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/camp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn camp .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/camp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gcamp .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gcamp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gcamp .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gcamp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gsito .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gsito_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gsito .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gsito_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gstig .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gstig_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gstig .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gstig_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lano .and. ( type C3 .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lano_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lano .and. ( type C3 .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lano_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgcamp .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgcamp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgcamp .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgcamp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgsito .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgsito_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgsito .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgsito_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgstig .and. ( type C3 .or. type 2O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgstig_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgstig .and. ( type C3 .or. type 2O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgstig_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspa .and. ( type P .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspa .and. ( type P .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yopa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yopa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dcpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dcpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dcpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dcpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pdopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pdopc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pdopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pdopc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn drpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/drpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn drpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/drpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yopc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yopc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dupc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dupc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dupc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dupc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddopc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddopc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddopc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pype_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pype_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pdope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pdope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pdope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pdope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dype_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dype_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oype_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oype .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oype_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddope_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddope .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddope_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dspg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dspg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pypg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pypg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sopg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sopg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dypg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn depg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/depg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn psps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/psps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn psps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/psps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pops_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pops_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sops_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sops_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn olps .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/olps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn olps .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/olps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dyps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dyps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dyps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dyps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yops_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn yops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/yops_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dops_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dops_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn deps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/deps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn deps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/deps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddops_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddops .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddops_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dnps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dnps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp1 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp1 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp2 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp2 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp3 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp3_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popp3 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popp3_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp1 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp1 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp2 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp2 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp3 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp3_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopp3 .and. ( type P+ .or. type C1 .or. type O1+','OP2+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopp3_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi24_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi24_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi25_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi25_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2a_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2a_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2b_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2b_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2c_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2c_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2d_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi2d_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi33_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi33_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi34_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi34_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi35_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpi35_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pypi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pypi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pypi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pypi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pspi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pspi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi24_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi24_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi25_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi25_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2a_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2a_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2b_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2b_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2c_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2c_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2d_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi2d_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi33_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi33_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi34_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi34_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi35_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popi35_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi24_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi24_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi25_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi25_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2a_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2a_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2b_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2b_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2c_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2c_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2d_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi2d_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi33_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi33_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi34_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi34_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi35_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plpi35_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi24_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi24_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi25_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi25_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2a_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2a_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2b_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2b_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2c_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2c_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2d_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi2d_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi33_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi33_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi34_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi34_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi35_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pnpi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pnpi35_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi13 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi14 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi15 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi24_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi24 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi24_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi25_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi25 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi25_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2a_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2a .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2a_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2b_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2b .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2b_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2c_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2c .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2c_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2d_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi2d .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi2d_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi33_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi33 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi33_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi34_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi34 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi34_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi35_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapi35 .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapi35_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlipi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlipi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn linpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/linpi_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn linpi .and. ( type P .or. type O1+ .or. type C1 .or. type C2 .or. type C3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/linpi_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tmcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tmcl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tmcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tmcl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tmcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tmcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tmcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tmcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmcl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmcl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paicl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paicl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paicl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paicl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pocl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pocl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pocl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pocl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pocl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pocl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pocl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pocl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tycl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tycl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tycl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tycl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tycl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tycl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tycl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tycl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tocl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tocl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tocl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tocl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tocl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tocl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tocl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tocl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loacl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loacl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loacl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loacl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loacl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loacl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loacl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loacl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loccl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loccl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loccl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loccl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loccl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loccl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn loccl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/loccl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tlcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tlcl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tlcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tlcl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tlcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tlcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tlcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tlcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnccl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnccl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnccl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnccl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnccl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnccl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnccl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnccl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnacl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnacl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnacl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnacl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnacl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnacl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnacl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnacl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lndcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lndcl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lndcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lndcl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lndcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lndcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lndcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lndcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnbcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnbcl1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnbcl1 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnbcl1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnbcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnbcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lnbcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lnbcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dlgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dlgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pogl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pogl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pogl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pogl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sogl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sogl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sogl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sogl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn slgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/slgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sagl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sagl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sagl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sagl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dygl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dygl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dygl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dygl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dogl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dogl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dogl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dogl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dggl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dggl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dggl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dggl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn degl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/degl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn degl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/degl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dngl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dngl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dngl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dngl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dtgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dtgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dtgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dtgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tigl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tigl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tigl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tigl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn digl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/digl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn digl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/digl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llgl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llgl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llgl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llgl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dagl .and. ( type O11 .or. type C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dagl_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dagl .and. ( type O11 .or. type C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dagl_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sape_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sape_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sapg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sapg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn saps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/saps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn saps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/saps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dape_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dape_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dapg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dapg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn daps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/daps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn daps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/daps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn peps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/peps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn peps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/peps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oeps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oeps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oeps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oeps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tspc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tspc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tspc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dtpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dtpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dtpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dtpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tipa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tipa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tipa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tipa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dipa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dipa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dipa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dipa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn llpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/llpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pleps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type OC+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pleps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pleps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type OC+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pleps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepa .and. ( type P .or. type C1 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plepg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plepg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pape_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pape .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pape_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paps .and. ( type P .or. type N .or. type C1+ .or. type O1+ .or. type O13% ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papa .and. ( type P .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn papa .and. ( type P .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/papa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lypg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lypg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lypg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dsm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tsm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tsm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn psm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/psm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn psm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/psm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ssm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ssm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ssm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ssm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn asm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/asm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn asm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/asm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bsm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bsm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn 23sm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/23sm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn 23sm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/23sm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lsm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lsm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn osm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/osm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn osm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/osm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nsm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nsm .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nsm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer160 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer160_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer160 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer160_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer180 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer180_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer180 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer180_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer181 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer181_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer181 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer181_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer200 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer200_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer200 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer200_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer220 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer220_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer220 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer220_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer240 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer240_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer240 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer240_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer241 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer241_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer241 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer241_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer3e .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer3e_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer3e .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer3e_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer6 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer6_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer6 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer6_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer3 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer3_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer3 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer3_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer1 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cer1 .and. ( type O1 .or. type C1S .or. type C2S .or. type C3S .or. type NF .or. type O3 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cer1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn maipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/maipe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aipe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn qmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/qmpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn qmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/qmpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn paipe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/paipe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn appc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/appc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn appc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/appc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ippc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ippc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ippc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ippc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pmpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pmpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn phpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/phpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn phpc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/phpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pppe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pppe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pppe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pppe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvpe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvpe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvpe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pvpg .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pvpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pepc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pepc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pepe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pepe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oepe_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oepe .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oepe_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oepc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn oepc .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/oepc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppgk .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppgk_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppgk .and. ( type P .or. type C1+ .or. type O1+ .or. type OC+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppgk_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgmar .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgmar_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgmar .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgmar_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arol .and. ( type C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arol_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arol .and. ( type C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arol_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgar .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgar_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pgar .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pgar_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pear .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pear_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pear .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pear_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men7 .and. ( type OR5 .or. type OR8 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men7_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men7 .and. ( type OR5 .or. type OR8 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men7_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men8 .and. ( type OR5 .or. type OR8 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men8_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men8 .and. ( type OR5 .or. type OR8 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men8_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men9 .and. ( type OR5 .or. type OR8 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men9_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn men9 .and. ( type OR5 .or. type OR8 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/men9_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mkol8 .and. ( type OR5 .or. type OR8 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mkol8_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mkol8 .and. ( type OR5 .or. type OR8 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mkol8_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arcl2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arcl2 .and. ( type P+ .or. type OP++ .or. type C1+ .or. type C2 .or. type C3+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arcl2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lau .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lau_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lau .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lau_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myr .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myr_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myr .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myr_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pal .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pal_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pal .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pal_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ste .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ste_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ste .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ste_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ara .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ara_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ara .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ara_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn beh .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/beh_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn beh .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/beh_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tri .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tri_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tri .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tri_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lign .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lign_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lign .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lign_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myro .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myro_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myro .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myro_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palo .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palo_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palo .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palo_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hta .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hta_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hta .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hta_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ole .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ole_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ole .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ole_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lin .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lin_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lin .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lin_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn alin .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/alin_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn alin .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/alin_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sda .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sda_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sda .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sda_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gla .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gla_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gla .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gla_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eico .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eico_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eico .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eico_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eda .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eda_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eda .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eda_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mea .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mea_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mea .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mea_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgla .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgla_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dgla .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dgla_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ete .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ete_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ete .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ete_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eta .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eta_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eta .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eta_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn epa .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/epa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn epa .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/epa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aran .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aran_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aran .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aran_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hpa .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hpa .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eru .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eru_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eru .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eru_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dda .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dda_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dda .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dda_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adr .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adr_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adr .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adr_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpt .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpt_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpt .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpt_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpa .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpa .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dha .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dha_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dha .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dha_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ner .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ner_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ner .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ner_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tta .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tta_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tta .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tta_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpt .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpt_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpt .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpt_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpa .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpa_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpa .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpa_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tha .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tha_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tha .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tha_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn laup .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/laup_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn laup .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/laup_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myrp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myrp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myrp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myrp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn step .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/step_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn step .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/step_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn arap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/arap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn behp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/behp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn behp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/behp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn trip .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/trip_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn trip .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/trip_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lignp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lignp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lignp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lignp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myrop .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myrop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn myrop .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/myrop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palop .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn palop .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/palop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn htap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/htap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn htap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/htap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn olep .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/olep_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn olep .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/olep_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn linp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/linp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn linp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/linp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn alinp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/alinp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn alinp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/alinp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sdap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sdap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn glap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/glap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn glap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/glap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eicop .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eicop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn eicop .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/eicop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn edap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/edap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn edap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/edap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn meap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/meap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn meap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/meap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dglap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dglap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dglap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dglap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn etep .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/etep_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn etep .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/etep_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn etap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/etap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn etap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/etap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn epap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/epap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn epap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/epap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aranp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aranp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aranp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aranp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hpap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hpap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn hpap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/hpap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn erup .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/erup_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn erup .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/erup_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adrp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adrp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adrp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adrp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dptp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dptp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dptp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dptp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dpap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dpap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dhap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dhap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dhap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dhap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nerp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nerp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nerp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nerp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ttap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ttap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ttap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ttap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tptp .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tptp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tptp .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tptp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tpap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tpap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn thap .and. ( type O1 .or. type O2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/thap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn thap .and. ( type O1 .or. type O2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/thap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-10 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-10 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-12 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-12_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-12 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-12_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-14 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sb3-14 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sb3-14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chaps .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chaps_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chaps .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chaps_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chapso .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chapso_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn chapso .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/chapso_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddmg .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddmg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddmg .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddmg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn domg .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/domg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn domg .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/domg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddao .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddao_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddao .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddao_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn udao .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/udao_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn udao .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/udao_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ldao .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ldao_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ldao .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ldao_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lapao .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lapao_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lapao .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lapao_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tripao .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tripao_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tripao .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tripao_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos10 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos10 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos12 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos12_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos12 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos12_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos13 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos13 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos14 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos14 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos15 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos15 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos16 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos16_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fos16 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fos16_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ufos10 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ufos10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ufos10 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ufos10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc12 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc12_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc12 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc12_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc14 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc14 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc16 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc16_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lpc16 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lpc16_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fois9 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fois9_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fois9 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fois9_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fois11 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fois11_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn fois11 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/fois11_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6dhpc .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6dhpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6dhpc .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6dhpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7dhpc .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7dhpc_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7dhpc .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7dhpc_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos3 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos3_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos3 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos3_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos4 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos4_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos4 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos4_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos5 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos5_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos5 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos5_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos6 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos6_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos6 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos6_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos7 .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos7_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn cyfos7 .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/cyfos7_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sds .and. ( type S ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sds_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn sds .and. ( type S ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/sds_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lmpg .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lmpg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lmpg .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lmpg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lppg .and. ( type P ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lppg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lppg .and. ( type P ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lppg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddaop .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddaop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ddaop .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ddaop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn udaop .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/udaop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn udaop .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/udaop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ldaop .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ldaop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ldaop .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ldaop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lapaop .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lapaop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn lapaop .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/lapaop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn trpaop .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/trpaop_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn trpaop .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/trpaop_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg3 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg3_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg3 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg3_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg4 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg4_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg4 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg4_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg5 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg5_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6eg5 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6eg5_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7eg4 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7eg4_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7eg4 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7eg4_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7eg5 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7eg5_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c7eg5 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c7eg5_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg4 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg4_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg4 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg4_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg5 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg5_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg5 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg5_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg6 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg6_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c8eg6 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c8eg6_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg5 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg5_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg5 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg5_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg6 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg6_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg6 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg6_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg9 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg9_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c10eg9 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c10eg9_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg7 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg7_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg7 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg7_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg8 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg8_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg8 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg8_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg9 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg9_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12eg9 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12eg9_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12deg .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12deg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c12deg .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c12deg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c13eg8 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c13eg8_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c13eg8 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c13eg8_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nidp40 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nidp40_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nidp40 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nidp40_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tx100 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tx100_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tx100 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tx100_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tx114 .and. ( type 1O1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tx114_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn tx114 .and. ( type 1O1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/tx114_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aog .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aog_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aog .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aog_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bog .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bog_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bog .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bog_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ang .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ang_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ang .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ang_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy3g .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy3g_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy3g .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy3g_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy3g .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy3g_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy3g .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy3g_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn amhcg .and. ( type N1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/amhcg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn amhcg .and. ( type N1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/amhcg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bmhcg .and. ( type N1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bmhcg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bmhcg .and. ( type N1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bmhcg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aom .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aom_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aom .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aom_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bom .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bom_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bom .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bom_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn anm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/anm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn anm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/anm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bnm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bnm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bnm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bnm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn audm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/audm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn audm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/audm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn budm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/budm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn budm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/budm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn a2udm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/a2udm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn a2udm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/a2udm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn b2udm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/b2udm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn b2udm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/b2udm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn admhm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/admhm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn admhm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/admhm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdmhm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdmhm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdmhm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdmhm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn appm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/appm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn appm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/appm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bppm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bppm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bppm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bppm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn atm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/atm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn atm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/atm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn btm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/btm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn btm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/btm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy3m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy3m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy3m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy3m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy3m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy3m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy3m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy3m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy4m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy4m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy4m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy4m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy4m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy4m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy4m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy4m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy5m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy5m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy5m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy5m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy5m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy5m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy5m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy5m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy6m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy6m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy6m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy6m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy6m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy6m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy6m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy6m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy7m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy7m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn acy7m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/acy7m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy7m .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy7m_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bcy7m .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bcy7m_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nheg .and. ( type NF ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nheg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nheg .and. ( type NF ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nheg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dheg .and. ( type NF ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dheg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dheg .and. ( type NF ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dheg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nmg .and. ( type NF ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nmg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn nmg .and. ( type NF ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/nmg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmg .and. ( type NF ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmg .and. ( type NF ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb10 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb10 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb11 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb11_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb11 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb11_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb12 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb12_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb12 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb12_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb13 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb13_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb13 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb13_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb14 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb14_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb14 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb14_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb15 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb15_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb15 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb15_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb16 .and. ( type N ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb16_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ctb16 .and. ( type N ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ctb16_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gms1 .and. ( type C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gms1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gms1 .and. ( type C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gms1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gms2 .and. ( type C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gms2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn gms2 .and. ( type C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/gms2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mln1 .and. ( type C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mln1_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mln1 .and. ( type C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mln1_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mln2 .and. ( type C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mln2_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mln2 .and. ( type C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mln2_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mgtm10 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mgtm10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mgtm10 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mgtm10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mgtm11 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mgtm11_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn mgtm11 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/mgtm11_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn egtm10 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/egtm10_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn egtm10 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/egtm10_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn egtm11 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/egtm11_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn egtm11 .and. ( type CC1 .or. type 3C2 .or. type 6C2 .or. type 9C2 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/egtm11_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn smdd .and. ( type 2C5 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/smdd_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn smdd .and. ( type 2C5 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/smdd_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bbphg .and. ( type 5C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bbphg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bbphg .and. ( type 5C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bbphg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bbcyg .and. ( type 5C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bbcyg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bbcyg .and. ( type 5C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bbcyg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bogng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bogng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bogng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bogng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn blmng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/blmng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn blmng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/blmng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdmng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdmng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdmng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdmng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c5mng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c5mng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c5mng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c5mng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6mng .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6mng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn c6mng .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/c6mng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botgng .and. ( type 2C* ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botgng_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botgng .and. ( type 2C* ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botgng_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn boing .and. ( type 2C* ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/boing_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn boing .and. ( type 2C* ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/boing_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ahtg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ahtg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn ahtg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/ahtg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bhtg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bhtg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bhtg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bhtg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aotg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aotg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aotg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aotg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botg .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botg_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botg .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botg_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aotm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aotm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn aotm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/aotm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn botm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/botm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn antm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/antm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn antm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/antm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bntm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bntm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bntm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bntm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn adtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/adtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bdtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bdtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn audtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/audtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn audtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/audtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn budtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/budtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn budtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/budtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn addtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/addtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddtm .and. ( type 2C1 ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddtm_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bddtm .and. ( type 2C1 ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bddtm_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dhpce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dhpce_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dhpce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dhpce_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popce_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popce_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopce_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopce_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpce_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpce .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpce_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popee_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn popee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/popee_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopee_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dopee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dopee_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppee_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dppee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dppee_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpee_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmpee .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmpee_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bmgp .and. ( type P .or. type C1 .or. type C11 .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bmgp_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn bmgp .and. ( type P .or. type C1 .or. type C11 .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/bmgp_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmtap .and. ( type N .or. type C1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmtap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dmtap .and. ( type N .or. type C1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dmtap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dptap .and. ( type N .or. type C1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dptap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dptap .and. ( type N .or. type C1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dptap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dotap .and. ( type N .or. type C1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dotap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dotap .and. ( type N .or. type C1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dotap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dstap .and. ( type N .or. type C1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dstap_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn dstap .and. ( type N .or. type C1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/dstap_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pla20 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pla20_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pla20 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pla20_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pla18 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pla18_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn pla18 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/pla18_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc22 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc22_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc22 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc22_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc20 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc20_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc20 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc20_head_lower.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc18 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .gt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc18_head_upper.ref
endif

define HEAD sele none end
define HEAD sele segid MEMB .and. resn plc18 .and. ( type P .or. type N .or. type C1+ .or. type O1+ ) .and. prop z .lt. 0 end
if ?nsel .ne. 0 then
   scalar wmain set 0 sele all end
   scalar wmain set 1 sele HEAD end
   write coor pdb name restraints/plc18_head_lower.ref
endif


